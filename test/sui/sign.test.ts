import { createKeypair, createKeypairFromSeed, createKeypairFromPrivateKey, createKeypairFromSeed2 } from '../../src/sui/address';
import * as bip39 from 'bip39';

describe('Sui Sign Module', () => {
  test('sui sign', () => {
    const kp = createKeypair();
  });

});